# figures

Generated figures. Every one is produced by a script in `../harness/` from
data in `../bench/data/`, and none is drawn by hand.

The rule exists so that a figure cannot drift away from the numbers it claims
to show. If a figure cannot be regenerated from a run directory, it does not
belong in the repository.
